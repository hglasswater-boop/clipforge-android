<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <queries>
        <package android:name="app.local1st.files" />
        <intent>
            <action android:name="app.local1st.files.action.PICK_FILES" />
        </intent>
        <intent>
            <action android:name="app.local1st.files.action.PICK_OUTPUT" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:label="ClipForge"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".processing.ClipForgeProcessingService"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing"
            android:stopWithTask="false" />

        <service
            android:name=".processing.AutoTrimAnalysisService"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing"
            android:stopWithTask="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".update.SelfUpdateInstallReceiver"
            android:exported="false" />
    </application>
</manifest>